import { subtract,dot,magnitude } from "../math/vector3.js"; import { degreesToRadians } from "../data/unitDefinitions.js";
export function enumerateAngles(molecule){const result=[];for(const center of molecule.getAtoms()){const neighbors=molecule.getNeighbors(center.id);for(let i=0;i<neighbors.length;i++)for(let j=i+1;j<neighbors.length;j++)result.push({atomA:neighbors[i],atomB:center,atomC:neighbors[j]});}return result;}
export function calculateAngle(atomA,atomB,atomC){const u=subtract(atomA.position_nm,atomB.position_nm),v=subtract(atomC.position_nm,atomB.position_nm),denominator=magnitude(u)*magnitude(v);if(denominator<=0)return {value:null,valid:false,warnings:["Vector angular degenerado"]};const cosine=Math.max(-1,Math.min(1,dot(u,v)/denominator));return {value:Math.acos(cosine),valid:true,warnings:[]};}
export function calculateAngleBending(interaction,parameter){const angle=calculateAngle(interaction.atomA,interaction.atomB,interaction.atomC);if(!parameter||!angle.valid)return {angle_rad:angle.value,energy_kJmol:null,valid:false,warnings:[...angle.warnings,"Parámetro angular faltante"],parameters:parameter??null};const displacement=angle.value-parameter.equilibriumAngle_rad;return {angle_rad:angle.value,equilibriumAngle_rad:parameter.equilibriumAngle_rad,displacement_rad:displacement,energy_kJmol:0.5*parameter.forceConstant_kJMolRad2*displacement**2,valid:true,warnings:angle.warnings,parameters:parameter};}
export const idealAngleByCoordination=coordination=>coordination<=2?Math.PI:coordination===3?2*Math.PI/3:degreesToRadians(109.47);
