import { subtract,cross,magnitude,dot,normalize } from "../math/vector3.js";
export function calculateDihedral(a,b,c,d){const b1=subtract(b.position_nm,a.position_nm),b2=subtract(c.position_nm,b.position_nm),b3=subtract(d.position_nm,c.position_nm),n1=cross(b1,b2),n2=cross(b2,b3);if(magnitude(n1)===0||magnitude(n2)===0)return {value:null,valid:false,warnings:["Planos torsionales degenerados"]};const b2n=normalize(b2),m1=cross(n1,b2n);return {value:Math.atan2(dot(m1,n2),dot(n1,n2)),valid:true,warnings:[]};}
export function calculateTorsion(interaction,parameter){const dihedral=calculateDihedral(interaction.a,interaction.b,interaction.c,interaction.d);if(!parameter||!dihedral.valid)return {dihedral_rad:dihedral.value,energy_kJmol:null,valid:false,warnings:[...dihedral.warnings,"Torsión sin parámetro suficiente"],parameters:parameter??null};const energy=parameter.terms.reduce((sum,t)=>sum+t.k_n*(1+Math.cos(t.n*dihedral.value-t.delta_rad)),0);return {dihedral_rad:dihedral.value,energy_kJmol:energy,valid:true,warnings:[],parameters:parameter};}
