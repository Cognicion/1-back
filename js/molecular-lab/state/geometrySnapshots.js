import { isFiniteVector3 } from "../math/vector3.js";
export function captureGeometrySnapshot(molecule,reason="manual") { return Object.freeze({ moleculeId:molecule.id??"current", atomPositions_nm:molecule.getAtoms().map(atom=>({id:atom.id,position_nm:{...atom.position_nm}})), createdAt:Date.now(), reason }); }
export function validateGeometrySnapshot(snapshot,molecule) { const errors=[]; if(!snapshot?.atomPositions_nm)errors.push("Snapshot inexistente."); else for(const entry of snapshot.atomPositions_nm){if(!molecule.getAtom(entry.id))errors.push(`Átomo ausente: ${entry.id}`);if(!isFiniteVector3(entry.position_nm))errors.push(`Coordenada no finita: ${entry.id}`);} return {valid:!errors.length,errors}; }
export function restoreGeometrySnapshot(snapshot,molecule,updateAtomPosition) { const validation=validateGeometrySnapshot(snapshot,molecule); if(!validation.valid)return validation; for(const entry of snapshot.atomPositions_nm)updateAtomPosition(entry.id,entry.position_nm,{allowFixed:true}); return {valid:true,errors:[]}; }
