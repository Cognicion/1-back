const text=result=>{if(!result)return "Seleccione de dos a cuatro átomos en la vista 3D.";if(!result.valid)return result.warnings.join(" ");if(result.type==="distance")return `${result.values.nm.toFixed(5)} nm · ${result.values.angstrom.toFixed(4)} Å`;if(result.type==="angle")return `${result.values.radians.toFixed(5)} rad · ${result.values.degrees.toFixed(3)}°`;return `${result.values.radians.toFixed(5)} rad · ${result.values.degrees.toFixed(3)}° · ${result.signConvention}`;};
export function renderMeasurementPanel(container,result,atomIds=[]){if(!container)return;container.textContent=text(result);container.dataset.measurementStatus=result?.valid?"valid":result?"warning":"idle";container.title=atomIds.join(" → ");}
