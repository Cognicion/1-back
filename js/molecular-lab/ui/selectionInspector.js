export function renderSelectionInspector(container,selection,state){if(!selection){container.textContent="Sin selección.";return;}if(selection.entityType==="atom"){const atom=state.molecule.getAtom(selection.atomId);container.textContent=`Átomo ${atom.id} · ${atom.elementId} · (${atom.position_nm.x.toFixed(4)}, ${atom.position_nm.y.toFixed(4)}, ${atom.position_nm.z.toFixed(4)}) nm${atom.fixed?" · fijo":""}`;return;}const bond=state.molecule.getBond(selection.bondId);container.textContent=`Enlace ${bond.id} · orden ${bond.order} · ${bond.atomAId} — ${bond.atomBId}`;}
