export function renderEnergyPanel(container,energy){if(!container)return;container.innerHTML=energy?`<div class="ml-status"><b>Energía total:</b> ${energy.totalEnergy_kJmol?.toFixed(6)??"no disponible"} kJ/mol<br><b>Enlaces:</b> ${energy.components.bond.energy_kJmol.toFixed(6)} · <b>Ángulos:</b> ${energy.components.angle.energy_kJmol.toFixed(6)} · <b>Coulomb:</b> ${energy.components.coulomb.energy_kJmol.toFixed(6)} · <b>Lennard-Jones:</b> ${energy.components.lennardJones.energy_kJmol.toFixed(6)} kJ/mol<br><b>Minimizable:</b> ${energy.minimizable?'sí':'no'}<br><b>Advertencias:</b> ${energy.warnings.length}</div>`:'<p class="ml-empty">Calcula la energía para mostrar sus términos.</p>';}
export function renderForcePanel(container,forces){if(!container)return;container.innerHTML=forces?`<div class="ml-status"><b>Fuerza máxima:</b> ${forces.maxForceMagnitude.toFixed(6)} kJ/mol/nm<br><b>RMS:</b> ${forces.rmsForce.toFixed(6)} kJ/mol/nm<br><b>Fuerza neta:</b> (${forces.netForce.x.toExponential(3)}, ${forces.netForce.y.toExponential(3)}, ${forces.netForce.z.toExponential(3)})</div><table class="ml-table"><tr><th>Átomo</th><th>Fx</th><th>Fy</th><th>Fz</th></tr>${Object.entries(forces.forcesByAtom).map(([id,f])=>`<tr><td>${id}</td><td>${f.x.toFixed(5)}</td><td>${f.y.toFixed(5)}</td><td>${f.z.toFixed(5)}</td></tr>`).join('')}</table>`:'<p class="ml-empty">Calcula las fuerzas para mostrar sus componentes.</p>';}
