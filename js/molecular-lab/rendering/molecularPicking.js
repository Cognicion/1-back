import * as THREE from "three"; export function createPicker(camera,domElement){const raycaster=new THREE.Raycaster(),pointer=new THREE.Vector2();return {pick(event,objects){const rect=domElement.getBoundingClientRect();pointer.x=((event.clientX-rect.left)/rect.width)*2-1;pointer.y=-((event.clientY-rect.top)/rect.height)*2+1;raycaster.setFromCamera(pointer,camera);return raycaster.intersectObjects(objects,true).find(hit=>hit.object.userData.entityType)||null;}};}
